<% include ./partials/headeradmin %>

<% if(currentUser && currentUser.isAdmin){ %>
              <div class="card mt-1 mb-5">
                <div class="card-body">
                    <div>
                        <h4>As an administrator you can manage <a href="/allEvents">events</a> and <a href="/allUsers">users</a>.</h4>
                    </div>
                </div>
              </div>
<% } %>

<% include ./partials/footeradmin %>